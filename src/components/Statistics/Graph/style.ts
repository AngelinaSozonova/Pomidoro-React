import styled from "styled-components";

export const wrapGraph = styled.div`
    height: 471px;
    flex: 1;
    background-color: ${(props) => props.theme.backgroundStatistics};
    padding: 20px 20px 0 0;
`;
